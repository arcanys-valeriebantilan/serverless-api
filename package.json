{
  "name": "sample-serverless-api",
  "version": "1.0.0",
  "description": "A sample serverless api endpoint",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "serverless offline start"
  },
  "author": "valeriebantilan",
  "license": "ISC",
  "devDependencies": {
    "serverless-offline": "^8.2.0"
  }
}
